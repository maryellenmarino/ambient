import { StyleSheet } from "react-native";
import { IPOD_WIDTH } from "./constants";

export const ipodBodyStyles = StyleSheet.create({
  ipodBody: {
    width: IPOD_WIDTH,
    alignItems: "center",
  },
});

